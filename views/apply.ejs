
    <%- include naver %>
    <div class="waper">
<!--　
<% if (message) {%>
<div class="alert alert-danger" role="alert"><%= message %></div>
<% }%>
-->
<% if (message) { %>
<div class="alert alert-warning alert-dismissible" role="alert">
  <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
  <strong>Warning!</strong><%= message %>
</div>
<% } %>
      <div class="container">

<% if (user.statu == 0) {%>
        <div class="row regist-table">
          <form role="form" method="post" action="/apply" class="col-md-6 col-md-offset-3">
            <div class="form-group">
              <label ><span class="glyphicon glyphicon-user"></span>姓名</label>
              <input name="name" type="text" class="form-control" placeholder="请输入真实姓名">
            </div>

            <div class="form-group">
              <label ><span class="glyphicon glyphicon-heart"></span>昵称</label>
              <input name="nick" type="text" class="form-control" placeholder="请输入比赛所用昵称">
            </div>
            <div class="form-group">
              <label ><span class="glyphicon glyphicon-road"></span>学院</label>
              <input name="college" type="text" class="form-control" placeholder="请输入学院全称">
            </div>
            <div class="form-group">
              <label ><span class="glyphicon glyphicon-th-list"></span>学号</label>
              <input name="studentID" type="text" class="form-control" placeholder="请输入学号">
            </div>
            <div class="form-group">
              <label ><span class="glyphicon glyphicon-phone"></span>联系电话</label>
              <input  name="tel" type="text" class="form-control" placeholder="请输入有效的联系电话">
            </div>
            <div class="form-group">
              <label ><span class="glyphicon glyphicon-tree-deciduous"></span>QQ</label>
              <input  name="qq" type="text" class="form-control" placeholder="请输入QQ号 ">  
            </div>
            <button type="submit" class="btn btn-default">Submit</button>
          </form>
<% } else if (user.statu == 1) {%>
<div class="jumbotron">
  <h1>Hello, world!</h1>
  <p>you had applyed for this contest, Please wait for the administrator to confirm your eligibility.</p>
</div>
<% } else {%>
<div class="jumbotron">
  <h1>Hello, world!</h1>
  <p>The administrator has confirmed the eligibility, 
Please concentrate on preparing for the competition.</p>
        </div>
      </div>
<% } %>

      </div>

      <%- include footer %>
